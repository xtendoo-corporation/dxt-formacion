<odoo>

    <template id="portal_my_leads" name="My Leads">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True"/>

            <t t-call="portal.portal_searchbar">
                <t t-set="title">Leads</t>
            </t>
            <t t-if="not leads">
                <p>There are currently no leads.</p>
            </t>
            <t t-if="leads" t-call="portal.portal_table">
                <thead>
                    <tr class="active">
                        <th>Lead #</th>
                        <th>Contact Name</th>
                    </tr>
                </thead>
                <tbody>
                    <t t-foreach="leads" t-as="lead">
                        <tr>
                            <td>
                                <a t-att-href="lead.get_portal_url()" t-att-title="lead.name">
                                    <t t-esc="lead.name" t-if="lead.name != '/'"/>
                                    <em t-else="">Draft lead</em>
                                </a>
                            </td>
                            <td>
                                <span t-field="lead.contact_name"/>
                            </td>
                        </tr>
                    </t>
                </tbody>
            </t>
        </t>
    </template>

</odoo>
